🤖 VizTeh Bot — бот заявок в Telegram 

Telegram-бот для сбора заявок от клиентов и передачи их менеджеру с удобным управлением статусом.  
Стек: aiogram 3 + PostgreSQL + asyncpg.

Возможности
- 📝 Форма заявки в диалоге (FSM)
- 👨‍💼 Режим менеджера по MANAGER_CHAT_ID
- 🔔 Отправка заявки менеджеру с inline-кнопкой статуса (закрыть / открыть)
- 🗃️ Сохранение заявок в PostgreSQL
- 🏭 Кнопка «О компании» — фото + описание + контакты

Сценарии

Пользователь
1) Нажимает «Оставить заявку»
2) Отвечает на вопросы бота
3) Получает подтверждение, что заявка отправлена менеджеру

Менеджер
- Получает заявку сообщением с кнопкой статуса
- Меняет статус — сообщение обновляется в чате

Технологии
- Python
- aiogram 3
- PostgreSQL
- asyncpg
- python-dotenv

Структура проекта
- bot.py — логика бота, роутеры, FSM-форма, отправка заявки менеджеру, обработка callback-кнопок статуса
- db.py — пул соединений + операции с заявками (create_ticket, get_ticket, set_ticket_status, …)
- config.py — загрузка переменных окружения из .env

Переменные окружения

Создай .env (не коммить в репозиторий):
- TOKEN
- MANAGER_CHAT_ID
- PHOTO_ID

База данных:
- DB_HOST
- DB_PORT
- DB_NAME
- DB_USER
- DB_PASSWORD

.env.example

TOKEN=YOUR_TOKEN_HERE
MANAGER_CHAT_ID=123456789
PHOTO_ID=YOUR_TELEGRAM_FILE_ID
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vizteh
DB_USER=vizteh
DB_PASSWORD=change_me

Подготовка базы данных

Нужна таблица tickets (минимальный набор полей):
- id (PK)
- user_id
- username
- name
- org
- city
- direction
- description
- phone
- manager_id
- status (например open/closed)
- close_at (timestamp, nullable)

Запуск локально
1) Установи зависимости из requirements.txt
2) Создай .env и заполни переменные
3) Подними PostgreSQL и создай таблицу tickets
4) Запусти bot.py

Как получить PHOTO_ID 📸
1) В коде бота нужно раскомментировать функцию, которая возвращает file_id (обработчик получения PHOTO_ID).
2) Отправь фото боту — он ответит file_id.
3) Сохрани это значение в .env как PHOTO_ID.

Деплой на VPS
- Установить Python и зависимости
- Настроить PostgreSQL (или подключиться к существующей базе)
- Создать .env на сервере
- Запускать через systemd, чтобы бот автоматически перезапускался

Частые проблемы
- Токен не подхватывается — проверь .env и название переменной TOKEN
- Менеджер не получает заявки — проверь, что MANAGER_CHAT_ID это числовой ID
- Ошибка подключения к БД — проверь параметры DB_* и доступ к Postgres
- Кнопки статуса не работают — проверь, что callback-data совпадает с обработчиком и роутер подключён

Лицензия
Смотри файл LICENSE.
